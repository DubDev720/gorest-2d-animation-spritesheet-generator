# Optional.
# The current local spritesheet and scene editing workflow does not require an API key.
# Add your own key only if you enable cloud image generation features.
GEMINI_API_KEY=""

# Optional local server port. Defaults to 3000.
PORT=3000
